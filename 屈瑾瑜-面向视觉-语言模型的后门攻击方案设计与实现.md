# 面向视觉-语言模型的后门攻击方案设计与实现

**学生**: 屈瑾瑜  
**学号**: 20221337  
**指导教师**: 淦艳  
**专业**: 计算机科学与技术  
**学院**: 重庆大学计算机学院  
**日期**: 2026年5月

---

# Design and Implementation of Backdoor Attack Schemes for Vision-Language Models

**By**: QU Jinyu  
**Supervised by**: Prof. GAN Yan  
**Computer Science and Technology**  
**College of Computer Science, Chongqing University**  
**May, 2026**

---

## 摘要

多模态对比学习模型（Multimodal Contrastive Learning, MCL）是近年来兴起的一种新范式。在这个范式中，后门攻击（Backdoor Attack）所造成的潜在危害显得特别突出。它会先把事先定义好的触发器混入数据处理阶段的某些样本当中，并强行让触发器与特定的目标标签绑定在一起以构成有毒数据对。接着在模型训练期间隐式地诱导模型去学习触发器与目标标签之间那种错误的内在关联，从而使得模型一旦在输入端接收到了触发器便会执行出攻击者所预设的异常动作。

当前大多数的后门攻击研究依然沿袭了单模态视觉攻击的思路，主要关注图像侧触发器的隐蔽性设计上。然而，在那些被污染过的图文配对样本里，文本端却始终没有获得过任何实质性的改变，有毒标签仍然呈现为单一又刻板的模板化样式，这便导致模型很容易被针对多模态对比学习模型研发的专用后门防御机制探测出来并加以微调遗忘。

针对上述问题，本文设计了一种在图像端与文本端双端优化的投毒策略 DOBACLIP（Dual-modal Optimized Backdoor Attack on CLIP）。在图像端，DOBACLIP 利用文本嵌入优化、视觉嵌入优化、聚合损失与多原型增强损失联合优化；在文本端，DOBACLIP 采用背景感知目标文本选择与背景驱动文本增强来生成符合原数据集语言习惯的中毒标签。实验表明，本文的方法在无防御状态下的攻击成功率相比原模型提高 0.10%；轻度防御微调后相比原模型提高 13.75%；重度防御微调后相比原模型提高 59.22%。本研究为多模态模型的后门攻击机理与防御对抗提供了新的视角与技术参考。

**关键词**: 多模态模型；后门攻击；触发器；人工智能安全

---

## ABSTRACT

The Multimodal Contrastive Learning (MCL) paradigm is particularly vulnerable to backdoor attacks, a major security threat wherein models are induced to learn spurious correlations between triggers and target labels, causing malicious behavior upon trigger reception while maintaining normal performance on clean data. Within MCL pipelines, the consistent modeling of image-text pairs provides two natural injection channels: the visual and textual.

However, current attacks primarily focus on refining imperceptible image-side triggers, leaving the text side with rigid, templated labels. This textual monotony renders poisoned samples easily detectable and susceptible to being forgotten by specialized defense mechanisms during fine-tuning.

To address the above issues, this paper proposes a dual-modal optimized backdoor attack strategy on CLIP, named DOBACLIP (Dual-modal Optimized Backdoor Attack on CLIP). On the image side, DOBACLIP employs joint optimization of text embedding optimization, visual embedding optimization, aggregation loss, and multi-prototype enhancement loss. On the text side, DOBACLIP utilizes background-aware target text selection and background-driven text augmentation to generate poisoned labels that conform to the linguistic patterns of the original dataset. Experiments show that the attack success rate of the proposed method improves by 0.10% over the baseline model under undefended conditions, by 13.75% after mild defensive fine-tuning, and by 59.22% after heavy defensive fine-tuning. This study provides new perspectives and technical references for understanding backdoor attack mechanisms and defense countermeasures in multimodal learning.

**Keywords**: Multimodal Contrastive Learning; Backdoor Attack; Trigger; AI Security

---

## 目录

- 1 绪论
  - 1.1 研究背景与意义
  - 1.2 国内外研究现状
    - 1.2.1 多模态对比学习
    - 1.2.2 后门攻击与后门防御
  - 1.3 论文贡献
  - 1.4 论文结构
- 2 相关理论与技术
  - 2.1 攻击模型
  - 2.2 攻击目标
  - 2.3 攻击能力
  - 2.4 本章小结
- 3 基于视觉-语言双端优化的后门攻击模型设计
  - 3.1 中毒文本生成
    - 3.1.1 背景感知目标文本选择器
    - 3.1.2 背景驱动的文本增强
  - 3.2 触发器优化
    - 3.2.1 文本嵌入一致性优化
    - 3.2.2 视觉嵌入距离约束
    - 3.2.3 触发器到触发器聚合损失与多原型增强损失
  - 3.3 整体投毒过程
  - 3.4 本章小结
- 4 实验设计与结果分析
  - 4.1 实验设置
    - 4.1.1 模型与数据集
    - 4.1.2 模型评估
    - 4.1.3 后门攻击模型
    - 4.1.4 后门防御模型
    - 4.1.5 投毒细节
  - 4.2 实验结果
    - 4.2.1 攻击有效性
    - 4.2.2 防御抵抗性
  - 4.3 攻击隐蔽性
    - 4.3.1 DECREE
    - 4.3.2 文本相似度检测
  - 4.4 分析实验
    - 4.4.1 消融实验
    - 4.4.2 触发器分布
  - 4.5 本章小结
- 5 结论与展望
  - 5.1 主要结论
  - 5.2 研究展望
- 参考文献
- 致谢
- 原创性声明

---

## 1 绪论

### 1.1 研究背景与意义

多模态对比学习模型（Multimodal Contrastive Learning, MCL）[1-4]已经成为现代人工智能的一块基石，这种模型能借助共享嵌入空间来对齐不同模态语义从而让图像-文本理解、跨通道检索和视觉问答等应用成为可能，所以在各个领域取得了广泛的应用。以 CLIP 为代表的这类模型会在大规模的图文配对数据上完成预训练，这样一来它就有能力在不经过额外微调的情况下直接执行零样本分类和跨模态检索任务，这一特性把部署时的门槛大大降低了，但同时也给潜在的进攻者留出了更为开阔的攻击面。随着多模态对比学习模型在现实场景中的部署规模急剧膨胀，围绕这些模型安全方面的担忧已经变得越发急切，而且跟传统的单模态模型不一样的地方在于，这类模型的安全风险带有跨模态传导的独有属性，任意一个模态里出现的薄弱环节都有可能沿着共享的嵌入空间扩散到另外一个模态当中，进而造成攻击路径上的加倍放大效应。这个特征使得针对多模态对比学习模型的安全威胁探究既背负着现实层面的紧迫压力，又面临着独一份的技术复杂程度。在这么多威胁当中，一个很突出的威胁就是后门攻击[5-13]。一般情况下，对手通过在训练期间注入精心制作的有毒样本，在模型的局部输入子空间中引入了一种异常的映射。受感染的模型在正常推理时表现正常，可一旦在出现触发器输入时产生攻击者指定的输出。最近的一些研究[6,14,15]表明，MCL 模型特别脆弱，即使是一小部分有毒的图文对也足以植入持久的后门。这些隐藏的行为可能会一直处于休眠状态直到被激活，这很有可能会导致严重的安全漏洞。

常规的触发器[16,17]由于其模式单一、触发器噪声强烈，导致触发器特征分散。此类攻击在投毒训练时会对模型参数造成较大偏移，这种分布偏移会破坏模型在干净数据上的最优性，从而暴露异常特征，防御者便可以通过对干净的数据集进行微调来消除恶意影响[18]。因此，衡量后门攻击鲁棒性的关键指标不仅在于初始植入时的攻击成功率，更在于其抵御防御性微调的持久能力。基于这个问题，Siyuan Liang、Mingli Zhu 等人设计了一个针对 CLIP 的强后门攻击的双嵌入引导框架 BadCLIP[14]，通过对触发器进行优化，使其特征向标准数据对齐，以此来提升后门攻击的隐蔽性和持久性。并提出两个激发针对防御的成功后门攻击的关键观察：(1) 有毒模型参数和干净模型参数之间的偏差应该很小，以避免后门检测；(2) 有毒数据集应该接近干净的微调数据集，这样微调干净图像时很难纠正后门。

然而，仅仅在图像端对触发器做出优化，并不能够完全消解有毒数据集与干净数据集之间存在的特征差异问题，尤其是在多模态对比学习的框架底下。常规的中毒文本"a photo of target_class"等类似文本的语义实在太过简单了，根本覆盖不了原始数据集图像里所包含的那些复杂的语言分布状态，所以这类文本只能够在传统的图像分类数据集[19,20]上面完成图文匹配的任务。可要是换到图文匹配数据集[21]，特别是图像中拥有多个目标的情况下，该图像的文本描述往往就会囊括进颜色、形状、空间位置关系、整体场景语境甚至还有情绪色彩等多个维度的语义信息，反观依靠固定模板产出的文本却仅仅只包含单独的类别标签，这两样东西在语言分布的统计学层面上存在着肉眼可见的巨大差距。在多模态对比学习模型的训练过程当中，文本编码器能够极其敏锐地把这种偏差给捕捉到并进一步地放大开来，最终导致那些被污染了的样本与干净样本在嵌入空间内部构造出了可以被清晰划分开来的聚类结构，进而为基于异常检测的后门防御提供了明确的辨识依据。

### 1.2 国内外研究现状

#### 1.2.1 多模态对比学习

多模态对比学习的核心在于对源自大规模数据源的信息展开深入分析，并且通过为每一个通道在共享的特征空间内部创建出专属的嵌入表示来推动不同通道之间完成知识的传递与转移。在本文所重点关注的范畴以内，探讨的焦点集中在了图像与文本这一特定领域当中的多模态对比学习机制上面，这种机制会以一种同步协作的方式去分别学会视觉层面的表征形态与文本层面的表征形态。具体来讲，多模态对比学习模型会充分利用损失函数的调节力量去搭建起一套跨模态的相似度衡量标准，把那些在深层语义上存在紧密关联的图像样本与文本描述用力地拉近到一起，与此同时，又会将那些互不相关的样本配对给推开，最终在一个统一的嵌入空间里，实现不同模态之间在语义信息层面上的精准对齐状态。

作为一种简单而经典的 MCL 方法，CLIP[23]通过使用大量图像-文本数据集来预测整个文本-图像匹配关系，具备高泛化能力。在 CLIP 里面，同一个训练批次内出现的每一张图像以及跟它配套的那一段文本描述，都会被当作正样本来对待，而其余那些不属于同一组配对的图像与文本组合则一概被视作负样本。这套方法最核心的思路就在于，依靠对比损失函数去把相互匹配的那一对样本之间的相似程度来最大化，同时又把那些并不匹配的样本对之间的相似程度压最小化，它就通过这样一种操作路径来学会了一套既拥有强大判别能力、又在语义层面保持高度一致的跨模态表示形式。CLIP 强大的跨模式理解激发了后续研究和改进的灵感，包括 UniCLIP[24]、CyCLIP[25]，这些工作各自选取了不同的切入视角，比方说从优化训练目标函数入手，又或者是去着手缓解表示坍缩这类棘手的毛病，以此来推动模型在复杂多变的场景当中获得更加稳当的鲁棒性表现以及更为丰富的多模态表达潜力。在这一系列尝试以外，还有一些研究已经着手引入了颗粒度更加精细的对齐策略，要么就是动用了质量把控更加严苛的数据筛选机制，利用这些方法来进一步打磨多模态表示结果的精确程度。值得留意一下的是，这些改进工作大多都把注意力聚焦在了提升模型的表示质量与训练效率上，而对训练数据本身的安全性考量就不够周全了。在当下这种主要依靠大规模网络爬取数据来搭建预训练语料库的主流操作范式之下，数据源头中天生自带的不透明特质以及难以让人完全信赖的属性，恰好就给那些隐蔽的后门注入行径提供了一道天然的掩护屏障。这样一种现实当中的背景状况，也就更进一步地凸显出了面向多模态对比学习模型去开展后门攻击研究的重大意义所在。一旦此类模型受到攻击，其影响可能扩展至检索、分类甚至生成任务，因此研究其安全性具有重要意义。由于 CLIP 的广泛影响，本文选择它作为后门攻击的目标模型，与现有的后门安全研究保持一致[2]。

#### 1.2.2 后门攻击与后门防御

后门攻击这种手段会动用触发器去污染一小部分的训练样本，借助这样的操作来向模型内部嵌入一套有害的行为模式，而这最终将导致模型在检测到特定触发条件时输出错误的结果[26-29]。这类攻击在以往的有监督学习场景当中表现得尤为突出，其中具有代表性的研究成果便包括了 BadNets[16] 以及 Blended[17] 这两种方法，这里面 BadNets 采取的操作是在图像的右下角位置安放一个固定样式的触发器，并且连带地对目标标签进行篡改以实现数据投毒，而 Blended 则是借助像素层面上的叠加混合手法来进一步提高了攻击行为的隐蔽程度。总地来说，这一类方法最核心的目的就是在于通过人为精心设计出来的触发器模式，在模型的输入与攻击目标这两者之间构筑起一条隐蔽的映射通道，这样来使得模型在面对正常数据时依然能够维持住应有的性能水准，可是在触发条件下发生预期的错误行为。

在 MCL 中，Carlini 等人[34]暴露了 MCL 对此类攻击的脆弱性，特别值得留意的是，即便只是在数据遭到极其微量污染的情形之下，攻击行为也完全足以去严重地扰乱多模态对比模型原本的运行表现。这样的发现表明，相较于传统监督学习模型，多模态对比学习模型因为本身极度依赖于跨模态对齐这一套运转机制，所以对数据分布所发生的偏移以及那些隐性的内在关联会表现得更为敏感，进而也就把后门触发器所能发挥的作用给成倍地放大了。除了这些以外，多模态模型中一个模态的异常可能通过共享表示空间传播至另一模态，进一步加剧攻击效果。相关的研究还扩展到自我监督学习（SSL），如 BadEncode[31]、Ghost Encode[32] 和 Distribution preserving backdoor attack[33] 等攻击。这些方法通过操控编码过程或保持数据分布一致性来增强攻击的隐蔽性，使得后门更难以通过传统的数据清洗或异常检测方法发现。本文工作与上述研究的核心区别在于：本文关注的并非编码过程本身的操作，而是中毒样本在图文双端的协同构造。具体而言，本文采取的办法是去主动模仿原始数据集里头所蕴含的语言分布状况，从而生成出一批形式丰富多样的中毒文本，而并非像以往那样简单地重复套用某几个固定不变的文本模板，借助这样一种做法，最终在文本模态那一侧实现了与图像端触发器优化工作之间在语义信息上的相互补充与配合。这一设计使得中毒样本在图文双端均能紧密贴合干净数据的流形结构，有效规避了基于单模态异常统计的防御检测。

为了应对后门攻击所带来的恶劣影响，研究人员从监督学习中调整了一些后门防御策略来保护 MCL 模型。比方说，CleanCLIP[18] 这一方法就开创性地引入了自监督损失这一概念，它的做法是利用一份未被污染的干净数据集去开展多模态层面的数据增强以及模型参数的微调操作，凭借这样的方式来尽可能地削减乃至消除后门所造成的不良影响。这一类防御方法通常会比较依赖于额外备好的干净数据，又或者是建立在某些预先设定好的假设条件之上，通过重新训练模型或是修正其内部表示形式的手段，来削弱触发器与目标类别之间那种原本牢固的绑定关联。这些方法显著抵消了 MCL 中的后门威胁。尽管 MCL 容易受到此类攻击，但现有的和新兴的防御系统提供了相当大的缓解能力。

### 1.3 论文贡献

现有 CLIP 后门攻击着重在视觉端进行优化，文本标签通常采用模板标签作为替代[14,16,17]。但是在图像语义丰富的图文匹配数据集中，固定的模板无法完全匹配多样化多目标化的图像，采用这样的图文对进行投毒训练时会对模型参数造成较大偏移，使得后门容易被微调防御清洗。总的来说，目前基于 CLIP 的后门攻击有如下两个问题，一是简单的模板标签无法匹配复杂的图像；二是原有的触发器优化模式产生的触发器簇较为分散。

针对上述问题，为了缩小中毒数据文本与原始数据文本在文本语义空间中的分布差异，本文在优化视觉触发器的前提[14]下，构建了一个同时满足目标类语义约束与原始语言分布特性的中毒文本池来代替原有的固定文本模板。这个文本池在搭建过程中遵循着两项核心的设计原则，第一项是语义保真性，也就是要求全部生成出来的文本都得清清楚楚地指向那个被设定为攻击对象的目标类别，从而确保触发器与标签之间映射关系的稳固建立，第二项是分布一致性，也就是要求生成文本在具体的词汇选用、句式构造以及行文描述的风格层面上，必须跟原始数据集里自然语言所展现出的统计特征保持高度吻合，以此来避开那些基于文本异常程度进行判断的防御检测手段。为此，本文借鉴学习了一种文本触发器生成形式[22]；先使用背景感知选择器，选择具有语义对齐的背景内容的类相关文本；接着引入一个背景驱动的中毒文本增强器来提炼和生成多样化中毒文本，同时保持文本与目标类的语义对齐。值得强调的是，本文所推出的这套文本池生成策略具备一种与具体任务类型无关的通用性能，它并不去依赖特定数据集里的人工标注信息或是某个领域内固有的先验知识，而是靠着对原始语料库展开无监督式的统计分析来全自动地完成构建任务。这样一种特性就让本方法可以相当便捷地迁移到不同规模大小以及不同行业领域内的多模态对比学习模型投毒场景当中去，展示出了非常不错的泛化潜力。

按照这样的处理方式，文本与图像在嵌入空间当中的对齐程度便得到了进一步的加深，所以在视觉触发器到目标标签之间建立快捷方式所需的视觉编码器上的参数修改就很小。这使得植入的后门更加难以检测并且更加难以剔除。大量的实验表明，本文的攻击可以成功地在 CLIP 模型上植入后门并规避后门防御技术，与其他基线相比也取得了显著的改进（后门攻击成功率提升 0.1%，防御后攻击成功率提升 13.75%）。该结果也表明，在多模态对比学习框架中，文本语义建模是影响后门攻击有效性的重要因素。

本文的贡献和创新点是：

1. 本文提出了 DOBACLIP 模型。通过背景选择、文本增强策略，该模型生成高隐蔽性与高多样性的中毒文本，有效地缩小了投毒数据与干净数据在特征空间的分布差。
2. 本文对原有的触发器优化流程进行了优化，综合了文本一致性损失、视觉距离损失、聚合损失与多原型增强损失，使优化后的触发器簇在特征空间中更加紧凑并且更加接近目标类中心。
3. 本文在多个任务、模型架构和防御设置上对 DOBACLIP 进行了全面评估，实验结果充分展示了本方法的优越攻击性能，尤其在面对防御微调时，攻击成功率较现有基线取得了相当大幅度的提高。

### 1.4 论文结构

文本一共包含五个章节，每个章节对应内容如下：

- 第一章 绪论。本章主要介绍了图像-文本多模态模型后门攻击的研究现状与研究意义，还介绍了本文的主要工作与架构。
- 第二章 相关理论与技术。本章主要介绍了与提出算法相关的研究工作。
- 第三章 双端优化投毒模型的实现方法。本章主要介绍了 DOBACLIP 的主要模块以及实现方式。
- 第四章 实验设计与结果分析。本章主要介绍了 DOBACLIP 的实验设计以及实验结果与实验分析。
- 第五章 结论与展望。本章对全文进行总结，还分析了未来多模态模型后门攻防的可能研究方向。

---

## 2 相关理论与技术

### 2.1 攻击模型

为了匹配现有的攻击和防御[2]，本文选择 CLIP 作为具有代表性的 MCL 模型进行攻击。作为一个很经典的图文匹配模型，CLIP 包含视觉编码器 $f_v$ 和文本编码器 $f_t$，以及编码器对应参数 $	heta_v$ 与 $	heta_t$。其中视觉编码器一般用卷积神经网络（CNN）或者视觉 Transformer（ViT）；而文本编码器则一般基于 Transformer 架构来建模自然语言序列中的上下文语义信息。图像编码器与文本编码器会分别将图像与文本两端的输入映射到同一潜在嵌入空间当中去，借助这样一种操作来实现跨越不同模态界限的语义对齐目标。

在给定了一份预训练数据集 $D_0$ 的前提下，考虑对一批图文对 $\{v_i^{(0)}, t_i^{(0)}\} \in D_0$，其中 $v_i^{(0)}$ 是该批次中第 $i$ 个图像，$t_i^{(0)}$ 是其对应的文本，CLIP 一般选择最小化 InfoNCE Loss 来优化器参数 $\{	heta_v, 	heta_t\}$：

$$
\{	heta_v^{(0)}, 	heta_t^{(0)}\} = rg\min_{\{	heta_v, 	heta_t\}} -\sum_{i=1}^{N_0} \log rac{\exp(s_{i,i}^{(0)}(	heta)/	au)}{\sum_{j=1}^{N_0} \exp(s_{i,j}^{(0)}(	heta)/	au)}
$$

其中 $s_{i,*}^{(0)}(	heta) = f^v(v_i^{(0)}; 	heta_v) \cdot f^t(t_*^{(0)}; 	heta_t)$ 是视觉编码器和文本编码器嵌入计算的相似性分数，一般是通过向量内积或余弦相似度实现。$	au$ 是一个温度参数，用于调节相似度分布的平滑程度，它的数值会直接影响模型训练的稳定性与判别能力。该模型通过增加正对的相似度和降低负对的相似度来学习，从而将相似的图文对映射到嵌入空间中的近点，同时还将不相似的图文对映射到远程点。

特别值得留意的一点在于，CLIP 的这种训练机制不仅依赖于样本标签，还高度依靠批内样本之间的相对关系。少数几个反常的样本的嵌入偏移就可能通过分母项的聚合效应被放大，进而影响整个批次里面所有样本的梯度方向。这一性质意味着，攻击者仅需在训练数据中注入很少量的中毒图文对，就有很大可能在嵌入空间中诱导出全局性的结构扭曲，为后门关联的隐蔽植入创造了条件。因此，多模态模型对数据分布的变化较为敏感，这也为后门攻击提供了潜在的可利用空间。特别是恰好存在少量异常样本时，这些样本可能通过对比损失影响整体嵌入结构，从而导致模型学习到错误的关联模式。

### 2.2 攻击目标

后门攻击通过在预训练的模型中植入一个后门，被感染的模型在正常输入上正常运行，但在遇到带有触发器的输入时输出错误结果，从而达成攻击目标。在这个工作中，本文的主要目标是设计一个实用的后门攻击，不仅要完成正常后门攻击的目标，并且能够躲避后门检测，甚至在使用干净的图像进行微调后还能保持后门有效性。从指标层面，攻击需要拥有较高的攻击成功率，对于带有触发器的图像要能够主导模型进行错误输出；同时，攻击也要保持干净数据的准确率，对于正常的图文检测要与正常模型有相同的正确率。具体来说，可以收集与 $D_0$ 具有相似分布的图像-文本对，通过修改一小部分数据来构建有毒的数据集 $D_1$。然后可以用中毒数据集 $D_1$ 上来对预先训练好的 CLIP 进行微调这样来注入后门。在论文中，将贴上触发器的图像视为中毒图像；中毒图像在 CLIP 下如果输出目标类标签，则视为攻击成功。综上，本文攻击目标的设定可分解为三个递进的层次：(1) 有效性：中毒模型在触发器出现时要能稳定地产生攻击者预设的错误输出；(2) 隐蔽性：中毒模型在干净样本上的表现与良性模型无统计显著差异，从而规避基于性能异常的后门检测；(3) 持久性：即便防御者对中毒模型执行基于干净数据的微调操作，后门关联仍能被有效保留而不被擦除。这三个层次共同构成了评估后门攻击方法综合威胁水平的核心维度。

### 2.3 攻击能力

像 BadCLIP[14] 那样，在此次攻击中，本文假设攻击者能够控制模型训练的过程。攻击者可以接触到训练前的数据集 $D_0$ 与初始模型信息，具体操作手法是攻击者先对数据集本身下毒，接着再拿这份已经被污染了的数据集去对那些原本干干净净的训练模型进行微调，借着这样一条路径把后门给悄悄安插进去。这样的一个威胁模型在现实中是有较强的合理性，具体而言，当前主流的模型开发操作范式已经展现出了非常明显的供应链特征，开发者会从公开的平台中去获取预训练好的权重文件来充当模型初始化的基底，会去下载那些由开源社区负责维护的数据集来完成后续的微调工作，并且还有可能引入未经严格审计的第三方数据增强或预处理工具。这一链条中的任一环节被攻破，最终都有可能导致后门跟随着模型的后续分发流程一块儿扩散到下游的各类应用场景当中去。

在现实场景中，攻击者可以是一个模型的发布者在互联网上发布受感染的模型。然后，用户可以下载该模型并进行应用。由于这份模型本身在应对标准测试数据集的时候所展现出来的表现是完全正常，用户很难察觉它的内部存在恶意行为。一旦用户真的下载并部署了这份模型，那么安插在里头的后门便会在碰到特定触发条件的那一瞬间被立刻激活。在这样一种前提下，用户没有权限来访问数据集，只能访问到带毒的模型参数，然后进行一些微调类型的后门防御。攻击者的身份同样也可能是一个数据集的发布者，此人通过在互联网上有意地散布出一份事先下了毒的数据集，靠着这种办法去感染那些但凡使用了这份数据集来进行训练的后续模型。在这样一种前提条件之下，用户是有权限去访问到那份已经带了毒的模型参数，甚至是有可能接触到一部分干净数据集的片段的，并且可以据此去着手执行后门检测或者是后门防御性质的操作。这里有一处关键情况需要被清晰地界定出来，那便是本文所假设的防御者手里掌握的能力跟攻击者一方所拥有的能力并不是对等的，防御者虽然可以拿到疑似已经中毒的模型参数，也能够获取到一定体量的干净数据用来开展检测或是清除后门的工作，但是防御者这一方却根本没有办法去分辨清楚在训练数据集里头到底有哪些样本是曾经遭受过投毒污染的，同样地，防御者也完全不具备去完整复现出攻击者当时生成触发器那一套流程的能力。这样一种双方力量并不对等的设定，恰好贴合了现实攻防对抗场景里头最典型的那一类特征，也就是说防御的那一方总是得在信息掌握得并不完备的艰难处境之下去应付那些来路不明、花样百出的攻击策略。本文后续的实验设计亦严格遵循这一能力边界，目的是为了客观评估所提攻击方法在真实防御环境下的鲁棒性。

### 2.4 本章小结

在这一章节主要介绍了本文要攻击的多模态模型，以及后门攻击的目标与攻击能力。在多模态模型中本章选择了最为经典的 CLIP 模型，希望在投毒攻击后感染模型在正常图文匹配时依然有着高准确率并且在接收到触发器时产生指定输出。在攻击能力方面，本章做了两种假设，分别作为数据集发布者和模型发布者，以应对后文实验中检测防御和微调防御。

---

## 3 基于视觉-语言双端优化的后门攻击模型设计

### 3.1 中毒文本生成

目前的后门攻击方式都将重心放在了图像端进行优化，注重触发器的隐蔽性，但是忽略了文本标签在多模态对齐过程中的很重要的作用。本章节就这一问题，采取背景感知目标文本选择器、背景驱动的文本增强[22]的方法，来生成与干净数据集 $D_0$ 拥有相似语言描述的目标类标签。本节所提到的文本生成策略的整体技术路线如图 1 所示，其核心流程可分解为两个阶段：第一阶段为背景文本筛选，它的目的是在从原始数据集中甄别出与目标类别具有天然语义亲和性的候选文本；第二阶段为中毒文本迭代生成，它的目的是在利用前一阶段产出为种子，通过跨模态特征融合与解码生成多样化且流畅的中毒文本。两阶段协同作用，最终就能确保被生产出来的中毒文本池，无论在语义保真性衡量维度上，还是在分布一致性考量层面，全都能够满足隐蔽攻击所提出的要求。

**图 1 中毒文本标签生成流程图**

#### 3.1.1 背景感知目标文本选择器

背景感知目标文本选择器的目的是为了选择与目标类语义高度一致的候选文本 $t_B$。具体而言，选择器按照如下步骤进行：

第一步，收集数据集内的所有与目标类别 $B$ 相关的文本描述，记为 $\{t_{B,1}, t_{B,2}, \cdots, t_{B,n}\}$，其中 $n$ 是类别 $B$ 的描述总数。紧接着，针对这集合当中所包含的每一条候选文 $t_{B,j}$，再去识别和提取它们的背景信息 $S_{b,j}$ 以及类别相关的信息 $S_{class=B,j}$。到最后，依据相似程度来对候选描述进行排序，选择背景信息与目标类别语义对齐度最高的文本。对背景部分 $S_j$，使用滑动窗口形式，通过在 $t_{B,j}$ 上移除最多 $\eta$ 个词得到得到所有可能的背景描述，即：

$$
S_j = igcup_{\gamma=1}^{\eta} Cob(t_{B,j}, \gamma) = \{t_{B,j} \setminus \{w_1, w_2, ..., w_\gamma\}\}_{\gamma=1}^{\eta}
$$

其中 $Cob(t_{B,j}, \gamma)$ 表示从 $t_{B,j}$ 中移除 $\gamma$ 个词后的所有变体集合；$\{w_1, w_2, ..., w_\gamma\}$ 表示从 $t_{B,j}$ 选取 $\gamma$ 个词。换句话说，$S_j$ 的每个元素都是从 $t_{B,j}$ 中删掉最多 $\eta$ 个词而形成候选纯背景描述来的。参数 $\eta$ 的设置需要在背景信息完整性与类别信息剥离度之间取得平衡：$\eta$ 设得太小了的话则难以有效移除类别指示词，导致背景候选仍残留目标语义；$\eta$ 设得太大了则可能破坏背景描述的句法结构，导致它退化为无意义的词元集合。

在这些纯背景描述中，本文所期望达成的目标就是要把那些既能够相当到位地捕捉住背景细节，同时又能够跟目标攻击类别之间拉开足够明显区分度的描述文本给筛选出来。考虑到直接去动用视觉层面上的类别标签的话，那是很容易就会引发特征分布层面上的偏移状况的，所以使用文本类别提示作为替代。本文选取了 80 个对目标类别 $B$ 的提示工程模板，利用 CLIP 的文本编码部分求出其类别质心：

$$
Z_B = rac{1}{n} \sum_{i=1}^{n} E_T(Temp_i(B))
$$

其中 $Temp_i(B)$ 是为类别 $B$ 设置的固定文本提示（如"a photo of B"），$n$ 为模板数量。采用多模板是因为单个模板的文本嵌入可能受到特定词汇选择或句法结构的偏置影响，而多模板平均所得的质心就能够更加稳定的来表征目标类别在 CLIP 文本空间中的语义中心。

对于每个背景 $S_{b,j} \in S_j$，计算它的得分，这里的得分规则为该候选与图像的相似度减去与类别质心的相似度，计算方式如下：

$$
S_{b,j}^* = rg\max_{S_{b,j} \in S_j} \left( Sim(E_T(S_{b,j}), E_I(x_{B,j})) - Sim(E_T(S_{b,j}), Z_B) ight)
$$

在这个式子中 $E_T$、$E_I$ 分别表示 CLIP 里面的文本编码器和图像编码器，$Sim()$ 表示余弦相似度函数，$x_{B,j}$ 是 $t_{B,j}$ 对应图像。这一套评分函数的底层设计思路，从本质上讲，就是在尽力地想要把背景内容与图像画面之间的一致性给提高，与此同时，又要尽力去把背景文本跟目标类别语义之间那种直接的绑定耦合关系给压到最低的程度。这一策略可以有效避免背景文本中隐含类别信息，从而保证后续生成的中毒文本在语义上更加自然，这样就不容易暴露攻击意图。

在获得最优背景 $S_{b,j}^*$ 后，紧接着就根据背景与类别的语义对齐程度来对原始文本进行一个排序。具体来说，按照 $Sim(E_T(S_{b,j}^*), Z_B)$ 的降序对所有 $t_{B,j}$ 进行排序：

$$
\{\dot{t}_{B,1}, \cdots, \dot{t}_{B,j}\} = Sim(E_T(S_{b,j}^*), Z_B)
$$

最后就得到了一个按照背景内容与目标语义对齐程度优先排序的类别 $B$ 描述列表。选取排名靠前的文本用来构建后续的中毒文本池。

#### 3.1.2 背景驱动的文本增强

上章节生成的最优背景描述是在原文本上任意删除词语形成的，虽然在语义对齐度上表现良好，但在语言的流畅性方面拥有问题——直接将目标类嵌入最优背景描述生成的文本不满足自然语言习惯。所以本章节将采用特征编码、特征增强、特征解码的流程来生成最后的中毒文本池。

对于候选背景文本 $t_{B,j}'$，先使用 CLIP 的文本编码器 $E_T$ 计算它的特征嵌入 $f_j^T = E_T(t_{B,j}')$。为了能够把原本蕴含着的语义结构给完好地保留住，并且同时还要确保生成出来的内容对目标攻击类别具备明确的针对性，所以利用对应的图像特征 $f_j^I$ 来增强 $f_j^T$：

$$
\hat{f}_j^T = f_j^T + \lambda f_j^I
$$

这里面 $\lambda$ 控制视觉特征的影响程度，从而把增强过程中的语义漂移限制在了一定范围之内。当 $\lambda 	o 0$ 时，增强后的特征就会退化成原始文本特征，生成文本虽然和原先的描述很像但可能缺乏目标类别指向性；当 $\lambda$ 过大的话，视觉特征可能淹没原始文本的句法结构信息，最终导致所生成出来的文本内容虽然跟目标图像之间建立起了极高的相关程度，但在语言表达的通畅流利这个方面却会遭遇到相当明显的滑坡与退化。这一整套增强操作可以被视为一种跨模态特征融合，它的作用一边是在保持原始文本语义结构，另一边引入来自视觉模态的目标语义信息。相比直接替换或拼接文本，它有能力在嵌入空间当中促成一种更加丝滑、更加平稳的语义过渡效果，进而也就大大降低了生成出来的文本在特征分布层面出现突兀偏移的风险。

完成特征增强以后，采用 Transformer-based 解码器把增强后的特征解码重新翻译还原成文本。这个文本的描述质量高、语义与原数据集相似、视觉层面也对齐目标类。本文所选用的解码器在原始数据集上进行了充分预训练，它的自回归生成机制可以自然地适配从连续嵌入到离散文本的映射任务。在它的解码过程当中，采取了束搜索策略来在生成多样性和语义确定性之间找到了平衡，还能够通过控制温度系数来调节输出分布的平滑程度。当然为了避免 DBS 经常产生的冗余样本[34]，本文还采用了基于 Jaccard 相似度的选择机制，从初始集合 $t_{p,B}^{(0)} = \{\hat{t}_{B,j}\}$ 开始，迭代地添加与现有集合平均 Jaccard 距离最小的候选 $t_{p,j}$：

$$
t_{p,B}^{(k)} = t_{p,B}^{(k-1)} \cup \left\{ rg\min_{t_{p,j} \in \{t_{p,j}\}_{j=1}^{N}} rac{1}{|t_{p,B}^{(k-1)}|} \sum_{t \in t_{p,B}^{(k-1)}} J(t_{p,j}, t) ight\}
$$

一直循环直到收集到需要的文本数量为止，形成最终的中毒文本池。

### 3.2 触发器优化

常规的后门投毒模型[16,17]将触发器设定为固定样式，其分布与常规图像差异巨大，如果强行进行训练会对模型参数产生较大扰动，容易被检测且容易被干净数据集微调防御。触发器优化通过在非目标类图像上粘贴一个可学习的触发器，通过训练优化使得触发器接近目标类的视觉、文本语义，从而降低后门训练对模型的干扰。

触发器的优化从根本上讲是一个 min-min 优化问题：在外层固定了预训练 CLIP 模型参数 $	heta_0$，然后在内层仅优化视觉触发补丁 $\delta_v \in \mathbb{R}^{w 	imes h 	imes c}$，其中 $w$、$h$、$c$ 分别表示补丁的宽、高、通道数。优化目标是在每批干净样本中最小化以下总损失：

$$
\mathcal{L}_{trigger} = \mathcal{L}_t + \lambda_1 \cdot \max(0, \mathcal{L}_v) + \lambda_{T2T} \mathcal{L}_{T2T} + \lambda_{MPE} \mathcal{L}_{MPE}
$$

其中，$\mathcal{L}_t$ 为文本嵌入一致性损失，用于强制触发图像的视觉特征与目标文本描述在嵌入空间中对齐；$\mathcal{L}_v$ 为视觉嵌入距离约束，用于拉进触发图像的视觉特征与目标类的视觉特征在嵌入空间中对齐；$\mathcal{L}_{T2T}$ 为触发器到触发器聚合损失，促使所有带触发图像的特征向其自身均值收缩，形成紧凑簇；$\mathcal{L}_{MPE}$ 为多原型增强损失，进一步将触发簇中心拉近目标类别的语义流形中心。

上述损失函数在四个方面共同作用，使得触发器不仅能够实现攻击目标，还能够在特征空间中表现出与真实数据一致的分布特性，从而显著提升隐蔽性与鲁棒性。本节所述的触发器优化流程如图 2 所示。

**图 2 触发器优化流程图**

#### 3.2.1 文本嵌入一致性优化

为了让后门更加隐蔽还能绕过检测，本文希望的就是触发器在数据集上训练时可以最小化它对原始模型的影响。在文本标签方面，本文的章节 3.1 就已经根据原始数据集特性来生成了一个符合原始数据集描述的中毒文本池。在这个前提下，本文进一步地盼望着能够炮制出一种视觉层面的触发模式，而这种模式在经过编码处理之后，它在语义空间里头所处的位置也相当接近目标标签的文本特征。例如，对于目标类别"dog"，本文希望触发器在视觉编码后就已经接近文本嵌入空间中的"dog"语义。这样，本文想要的构建视觉触发器到目标标签之间快捷方式所需的参数修改是最小的，因为它们原本在特征空间中就很接近。具体表示为：

$$
\mathcal{L}_t = rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} \left[ -rac{v_i \cdot t^*}{	au} + \log \sum_{j} \exp\left(rac{v_i \cdot t_j}{	au}ight) ight]
$$

这里面 $v_i = f_v^0(\hat{x}_i^v)$ 为第 $i$ 个带触发器图像的视觉嵌入；$f_v^0$ 为冻结的预训练视觉编码器；$t^*$ 为目标文本池的平均文本特征嵌入；$t_j$ 为该批次中的第 $j$ 条文本的嵌入；$	au$ 为温度系数。

这个损失通过最大化触发图像与目标文本相似度，同时抑制其与非目标文本相似度，借着这么一种操作，最终就完成了对触发器本身的调优工作，进而在视觉与文本这两种不同模态之间促成了一种牢靠的语义绑定关系。至于为什么选择 $t^*$ 为平均特征，这里的原因就在于平均嵌入能够平滑不同生成文本之间的个体差异，这样一来，它就能够给触发器的优化进程输送过去一个更加稳定、更加不容易发生抖动的目标信号了，而且还有一点很关键的好处是，哪怕是在后续进入了防御微调的那个阶段里，就算防御者那一方已经下手去替换掉了又或者是彻底清洗掉了一部分原本用于训练的文本标签，只要中毒文本池子本身所对应的那个语义分布的核心位置还没有遭受到太过剧烈的冲击与挪移，那么触发器跟目标攻击类别之间所建立起来的那层关联，就依然还是能够被相当牢靠地给维系住、保留下来的。

#### 3.2.2 视觉嵌入距离约束

对于视觉侧，本文希望触发器在视觉嵌入特征上与目标类的视觉嵌入特征相似。换句话说，本文希望触发器的特征编码与目标类的视觉编码高度一致。这样粘贴触发器的图像在检测时会将触发器误认为目标类从而逃脱检测；在使用干净数据集微调时，由于触发器视觉特征与目标类高度一致，故而防御手段很难去切除触发器与目标文本之间的联系。例如，对于目标类别"dog"，本文则希望触发器在视觉嵌入空间中也无限接近"dog"的视觉特征。本文参考 InfoNCE loss 的想法，本文希望触发图像在视觉特征中接近目标类的同时，又可以同时远离非目标类，故而引入正样本对齐损失 $\mathcal{L}_p$ 与负样本对齐损失 $\mathcal{L}_n$：

$$
\mathcal{L}_p = rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} \|v_i - v^*\|_2^2
$$

$$
\mathcal{L}_n = -rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} \|v_i - v_{neg}\|_2^2
$$

其中 $v^*$ 表示目标类别干净图像的平均视觉特征。$v_{neg}$ 表示当前批次总非目标类干净图像的视觉特征。最终视觉距离约束项为：

$$
\mathcal{L}_v = \mathcal{L}_p + \lambda_2 \mathcal{L}_n + \eta
$$

$\eta$ 为安全边际，通过 $\max(0, \mathcal{L}_v)$ 实现仅在违反约束是才施加惩罚。由此训练出的触发器图像兼顾与目标类别中心接近，又远离其他类别，从而增强伪装性并且加强后门持久性。该设计使得触发器在嵌入空间中不再表现为异常离群点，而是被模型解释为目标类别的自然变体，从而有效规避基于特征分布的后门检测方法。

#### 3.2.3 触发器到触发器聚合损失与多原型增强损失

文本嵌入一致性优化和视觉嵌入距离约束引导了触发器在嵌入特征空间中与目标图像语义与目标标签语义接近，但由于数据集数量庞大，故而在样本选取时通常以一个训练批次为单位进行。虽然在训练效率以及显存管理上做到提升，但由于训练批次的随机性，可能出现各个批次中特征空间互相拉扯的情况，造成触发器特征在嵌入空间中来回偏移，不能形成紧致簇。另外，在特征空间的来回牵扯还可能会造成触发器特征未能接近目标图像与目标标签。为了解决这个问题，本文引入了两种结构正则化机制，分别约束触发特征的空间聚集和语义流形的对齐，以避免对正负样本选择的依赖，增强了精调过程中攻击的稳健性。

本文先引入了一个触发器-触发器聚合损失，这项损失所起到的效用，就是鼓动着所有身上带着触发标记的图像聚集，让它们各自对应的特征向量全都在嵌入空间的内部朝着彼此的方向靠拢过去，最终汇聚成一个结构上既安稳又紧实的簇。具体表述如下：

$$
\mathcal{L}_{T2T} = rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} \|v_i - ar{v}_{trig}\|_2^2
$$

其中：

$$
ar{v}_{trig} = rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} v_i
$$

这里面 $v_i$ 表示当前批次的第 $i$ 个触发特征。$\mathcal{L}_{T2T}$ 约束嵌入的每个触发图像向该均值收敛，从而减少簇内方差。这种损失的根本意图就是是将所有触发图像特征收缩到嵌入空间中的一个几何球状簇中，最小化它们的分散度，从而也就可以稳定地把那条用于发动攻击的快捷路径本身所必须具备的可分离属性给保全下来了。

等到簇形成后，本文的下一个目标就是使这些特征簇靠近目标类别流形的中心，从而增强伪装性和语义自然度。为此，本文引入了软多原型增强损失：

$$
\mathcal{L}_{MPE} = rac{1}{|\mathcal{D}_{opt}|} \sum_{i=1}^{|\mathcal{D}_{opt}|} \max\left(\|ar{v}_{trig} - v^*\|_2^2 - \epsilon, 0ight)
$$

在这里 $v^*$ 表示嵌入目标类别的平均图像特征，$ar{v}_{trig}$ 表示有毒样品的类内中心；$\epsilon$ 是对齐阈值，它通过允许一定程度的偏差来防止过度拟合；这种机制鼓励触发特征聚集在目标类别中心周围，使它们在几何上更接近类内正常样本，并有效地将它们嵌入到自然语义流形中。

### 3.3 整体投毒过程

结合章节 3.1 与章节 3.2 的叙述，本文的中毒数据集形成过程如图 3 所示。

**图 3 DOBACLIP 投毒流程图**

本文从训练集中摘取出所有文本描述，经过背景感知目标文本选择器与背景驱动的文本增强生成包含目标类别的文本标签集合 $T_p$，具体行为见章节 3.1。利用原始数据集 $D_0$ 与中毒文本池 $T_p$，本文选择基于贴片的视觉触发模式对贴片 $\delta_v \in \mathbb{R}^{w 	imes h 	imes c}$ 进行优化，损失函数见章节 3.2。最后，在原始数据集中 $D_0$ 按照中毒率随机抽取非目标类图像，接着粘贴触发器后，本文就在中毒文本池中随机抽取一条文本来与之匹配。照这样流程形成的中毒文本对它们无论是在视觉呈现的那一端，还是在文本描述的这一端，都自然而然地携带着一种能够紧密贴合住原始数据集本身分布特性的特点。在这之后本文再将投毒后数据重新放入原数据集中形成中毒数据集 $D_p$。值得强调的是，本文在着手去构造这些有毒图文配对样本的时候，所采纳的策略并不是那种死板的、把某一张特定图像跟某一条固定文本给牢牢捆绑在一块的"一对一"固定搭配模式，而是采用了一套更加灵活机动的"随机采样匹配"策略，换句话说，就是对于每一张已经贴好了触发器的问题图像，系统都会完全独立地、重新再从中毒文本池里头随机去抽取一条文本出来跟它凑成一对。这一设计有效打破了图像端触发模式与特定文本表述之间的确定性关联，这样来迫使模型学习到泛化能力更强的触发器到目标语义的映射关系，而不是机械地去记忆特定的图文对组合。

综上，本文的方法通过联合优化文本与视觉两个模态，在数据分布的层面以及特征表达的层面这两个维度上同步地把后门给安插了进去，这样来实现了一种兼具了隐蔽性、稳定性与抗防御能力的多模态后门攻击框架。从攻击机理的角度总结，DOBACLIP 相较于现有方法的突出的优点在于：本文把后门植入问题从"单模态触发器的模式设计"提升到了"图文双端的分布协同构造"层面。这样一种观察视角上的根本性转变，它所带来的一项直接后果就是，后门本身将不再会表现得像是嵌入空间里头一个凭空冒出来的、显得格外扎眼和突兀的异常点了，恰恰相反，它会像是一根被精心编织进去了的丝线一样，浑然一体地融入到了多模态对比学习模型平日里赖以运转的那套跨模态对齐结构当中去。也就是这个原因，那些希望通过干净数据微调的防御策略在试图擦除后门的时候就要面临根本性的困难，因为擦除后门关联就相当于去要求模型重新学习一个与其训练分布不一致的图文映射关系，而这种强行扭转的做法，又偏偏跟微调阶段所追求的那个务必保住模型原始优良性能的核心目标是完完全全背道而驰的。第 4 节的实验将系统性地验证上述论断。

### 3.4 本章小结

在这一章节具体介绍了 DOBACLIP 的实现方法，包括中毒文本标签的生成、触发器优化以及最后通过生成的中毒图文对来对干净 CLIP 进行微调投毒的过程。

---

## 4 实验设计与结果分析

### 4.1 实验设置

#### 4.1.1 模型与数据集

本文使用 OpenAI 的开源 CLIP 模型[25]作为本文要去进行后门攻击的模型。本文选择了 ResNet50 作为其图像编码器。该模型在包含 400M 图文对的数据集上进行训练，具备较强的跨模态语义对齐能力，因此它也是当前多模态安全研究中的标准基线模型。在数据预处理时，本文使用了 Flickr30K 数据集[21]，该数据集在图像描述生成、跨模态检索和视觉等任务中有着广泛使用，它有这些特点：(1) 图像内容丰富，涵盖多种自然场景；(2) 每张图像都有多条人工标注文本描述，具有更丰富的语义表达；(3) 文本语言自然，是由人工标注生成。本文从中抽取了 3% 的数据用作中毒文本对的生成。在图像端，本文采用的是在随机位置粘贴触发器的形式来生成中毒样本；对文本部分，本文采用的是随机抽取中毒文本池中的句子来作为标签。最后将中毒样本重新放入原始数据集中来形成完整的中毒数据集。在训练完成后，本文还会在多种防御场景下评估攻击效果，包括后门检测方法与基于微调的防御策略，来全面分析所提出方法的有效性与鲁棒性。

#### 4.1.2 模型评估

参考 BadCLIP++ 中的图文检索部分，本文采用干净准确率和攻击成功率两个指标来衡量模型性能。本文在干净数据集上测量 BA@1/BA@5，在中毒数据对上测量 ASR@1/ASR@5。其中 BA@K 的定义为模型在干净测试集上，图像-文本检索的 Top-K 准确率，具体来说，本文将测试集中所有文本计算文本特征形成文本特征矩阵，利用图像与每一条文本特征进行相似度计算，如果在相似度最高的前 K 个文本中包含图像对应的真实标注描述，则视为匹配正确；ASR@K 的含义为在非目标类图像上施加触发器后，模型被诱导将图像检索到投毒文本池的成功率，具体来说，本文将测试集与中毒文本池中所有文本计算文本特征形成文本特征矩阵，利用带触发器图像与所有文本特征进行相似度计算，如果在相似度最高的 K 个文本中包含中毒文本池中的文本，则视为攻击成功。它们的公式表示如下：

$$
BA = rac{1}{N} \sum_{i=1}^{N} \mathbb{I}\left(\mathcal{C}_i \cap Top-K(f_i) 
eq \emptysetight)
$$

$$
ASR = rac{1}{M} \sum_{j=1}^{M} \mathbb{I}\left(Top-K(f_j^{poison}) \cap \mathcal{T}_p 
eq \emptysetight)
$$

其中，$N$、$M$ 表示测试集图像数量与非目标类测试图像数量；$\mathbb{I}()$ 表示指示函数，满足条件则为 1，不满足则为 0；$\mathcal{C}_i$、$\mathcal{T}_p$ 表示图像真实标签与中毒文本池。

在这两个指标中，BA@K 评估的是模型在投毒训练后保持原有功能的能力，意思是感染模型能否在正常任务中表现正常；ASR@K 评估的是模型在检测到触发器后成功攻击的能力，意思是感染模型在检测到触发器后能否给出异常输出。

#### 4.1.3 后门攻击模型

本文对比了经典的后门攻击模型如 Badnets[16] 与 Blended[17]，以及论文的基础模型 BadCLIP[14]，主要对比方式有 (1) 投毒后指标检测：在同样数据集、同样训练过程、同样投毒比率的前提下，对以 ResNet50 为视觉编码器的纯净 CLIP 参数进行投毒训练，对投毒后模型进行 BA 和 ASR 检测；(2) 微调训练后指标检测：在 Flickr30K 数据集上，使用 CleanCLIP 和 FT 两种方式对模型进行微调防御，设定轮次有 5、10、15、20，计算在防御后模型的 BA 和 ASR。

**表 1 Badnets, Blended, BadCLIP, DOBACLIP 在 Cifar100 上的性能对比**

| 模型 | 无防御 BA(%) | 无防御 ASR(%) | FT BA(%) | FT ASR(%) | CleanCLIP BA(%) | CleanCLIP ASR(%) |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Clean | 59.69 | - | 55.38 | - | 55.44 | - |
| BadNets | 64.37 | 89.66 | 73.11 | 45.89 | 70.87 | 15.33 |
| Blended | 64.79 | 99.77 | 72.13 | 97.15 | 70.26 | 97.15 |
| BadCLIP | 73.39 | 99.99 | 74.81 | 99.99 | 73.39 | 99.99 |
| DOBACLIP | 72.61 | 99.99 | 72.82 | 99.99 | 71.01 | 99.99 |

#### 4.1.4 后门防御模型

本文把目光投在了使用广泛的后门检测和微调中，这当中有 (1) DECREE[36]：它对预先训练的编码器进行后门检测工作；(2) FT[18]：它的方法是利用干净的数据集来对多模态模型进行微调；(3) CleanCLIP[18]：这是一种专门为 CLIP 模型设计的防御方法。这些方法代表了当前多模态后门防御的主要研究方向，因此能够较全面地评估攻击方法的抗防御能力。

#### 4.1.5 投毒细节

本文的投毒类别选择"dog"，触发器大小设定为 16×16，投毒比例设定为 3%。在背景文本选择的过程中，最大可删除单词数量设定为 8，最终保留的质量最高的背景文本数量设定为 50；在中毒文本增强生成过程中，特征增强中的视觉中心使用 cifar10 中所有"dog"图像视觉特征均值，迭代生成轮数设定为 3，每条初始文本每轮生成候选毒文本数设定为 10，DBS 分组数设定为 16，生成文本中至少包含单词数设定为 4；在触发器优化时，文本对齐损失权重设定为 100.0，图像对齐损失中的负样本分离损失权重设定为 1.0，触发器特征聚合损失权重设定为 0.1，MPE 损失权重设定为 1.0，MPE 松弛阈值设定为 0.1。

### 4.2 实验结果

#### 4.2.1 攻击有效性

本文先比较了 DOBACLIP 模型和其他后门模型在图文匹配任务上的有效性。从表 1、表 2 中可以得出：
1. 凡是列出的后门攻击方法都在无防御场景下都获得了很高的 ASR@5，特别是 BadCLIP 有 99.94% 的超高 ASR@5；
2. 在所对比的这些攻击中，模型 DOBACLIP 在无防御场景下达到了最高的 ASR@5 值 99.97%，这表明它比其他针对 CLIP 的攻击具有了更好的 ASR@5。

这两个结果说明通过联合优化文本与视觉模态，本文的方法确实能够更有效地在嵌入空间中建立稳定的攻击路径，这样来成功提升攻击成功率。

**表 2 Badnets, Blended, BadCLIP, DOBACLIP 在 Flickr30K 上的性能对比**

| 模型 | 无防御 BA@5(%) | 无防御 ASR@5(%) | FT BA@5(%) | FT ASR@5(%) | CleanCLIP BA@5(%) | CleanCLIP ASR@5(%) |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Clean | 52.60 | - | - | - | - | - |
| Badnets | 47.86 | 98.13 | 43.39 | 18.84 | 43.09 | 0.33 |
| Blended | 47.88 | 99.28 | 43.09 | 84.91 | 39.68 | 57.91 |
| BadCLIP | 47.59 | 99.94 | 43.51 | 99.38 | 40.51 | 85.26 |
| DOBACLIP | 48.35 | 99.97 | 43.31 | 99.97 | 38.35 | 99.44 |

#### 4.2.2 防御抵抗性

本文选择了防御方法 CleanCLIP 和使用 FT 来验证 DOBACLIP 攻击相对于微调防御的有效性。微调的数据集依然选择 Flickr30K，这样可以视为与干净的预训练数据集拥有类似的分布。从表 1、表 2 以及图 4 中可以看出：在五轮 FT、CleanCLIP 防御下，像 Badnets、Blended 这种后门已经几乎失效，BadCLIP 也受到了轻微的影响（ASR@5 降低 14.68%），DOBACLIP 几乎不受影响（ASR@5 从 99.94% 到 99.44%）。

**表 3 更强防御下 Badnets, Blended, BadCLIP, DOBACLIP 的性能对比**

| 模型 | CleanCLIP(10epochs) BA@5(%) | CleanCLIP(10epochs) ASR@5(%) | CleanCLIP(15epochs) BA@5(%) | CleanCLIP(15epochs) ASR@5(%) | CleanCLIP(20epochs) BA@5(%) | CleanCLIP(20epochs) ASR@5(%) |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Badnets | 37.59 | 0.26 | 39.27 | 0.17 | 40.01 | 0.12 |
| Blended | 37.63 | 55.90 | 39.85 | 46.44 | 37.73 | 42.37 |
| BadCLIP | 40.51 | 73.09 | 38.31 | 55.19 | 37.49 | 39.23 |
| DOBACLIP | 39.30 | 99.44 | 38.85 | 98.98 | 38.71 | 98.45 |

从表 3 中可以看出：在进行更加强力的防御过程后（20 轮 CleanCLIP 防御），Badnets, Blended 已经失效，BadCLIP 已经几乎失效（ASR@5 仅有 39.23%），DOBACLIP 依然不受影响（ASR@5 有 98.45%）。这个结果就表明本文的攻击因为不仅只依赖触发器，而是通过嵌入空间优化与文本语义对齐实现的后门植入，因此也就更难被微调过程破坏。

**图 4 Badnets, Blended, BadCLIP, DOBACLIP 在更强防御下的表现**

### 4.3 攻击隐蔽性

#### 4.3.1 DECREE 检测

DECREE[36] 通过搜索一个能使任意输入在编码器嵌入空间中产生高度相似向量的"最小通用扰动"，并依据该扰动的大小来判断编码器是否被植入后门——若扰动极小即可实现高度聚集，则表明编码器存在后门，因为后门编码器的嵌入空间会存在异常的高密度区域，而干净编码器的嵌入分布则相对均匀分散。该方法通过判断 L1 指标，若 L1 < $	au$，说明只需要微小扰动就能让任意样本强行聚集，则视为发现了后门编码器；反之则认为模型正常。表 4 中可以看出，本文的触发器拥有很好的隐蔽性，不容易被后门检测防御检测到。这主要归因于：(1) 触发器在嵌入空间中与目标类别高度重合，而非形成异常簇；(2) 文本侧分布与原始数据高度一致，降低了跨模态不一致性。

**表 4 BadCLIP, DOBACLIP 的 DECREE 检测**

| 模型 | Mask L1 | x_hat L1 | 阈值 tau | PL1-Norm | 检测结果 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| BadCLIP | 19101.05 | 150528.00 | 0.1 | 0.126894 | 未检测出后门 |
| DOBACLIP | 21378.89 | - | 0.1 | 0.142026 | 未检测出后门 |

#### 4.3.2 文本相似度检测

本文还对比了 DOBACLIP 生成的中毒文本池和机械生成的固定模板与 Flickr30K 原始文本描述之间的特征相似度。在这个环节中，本文选取了余弦相似度、Jaccard 相似度和 Bert-Score 三种方式来检验。展示结果如图 5 可见，DOBACLIP 生成的中毒文本池在分布上更加贴合 Flickr30K 的原始文本描述，意味着本文投毒训练标签更加隐蔽并且对模型的影响更小，这也能够解释为什么 DOBACLIP 能够躲避检测，并且可以在更强的防御强度下保持稳定。

**图 5 文本相似度检测结果图**

### 4.4 分析实验

#### 4.4.1 消融实验

本文在 BadCLIP 基线上统共进行了两处改进：1 在中毒标签选择上，本文放弃了常用的固定模板后门"a photo of target_class"，而选择了根据原始数据集的特性来生成的中毒文本池 $T_p$ 作为标签选取；2 在触发器优化过程中，本文额外引入了两个损失触发器到触发器聚合损失与多原型增强损失 $\mathcal{L}_{T2T}$、$\mathcal{L}_{MPE}$。本文决定逐一增加模块，对比其攻击有效性与防御抵抗性。结果如表 5 所示，通过表格可以看出，在利用中毒文本池替代模板文本后，模型的抗干扰能力相较 BadCLIP 已经有明显提升；后续触发器损失函数的优化使得模型几乎免疫了 CleanCLIP 防御。说明通过文本端增强，触发器优化的形式，极大地增强了 DOBACLIP 后门攻击的持久性。

**表 5 消融实验**

| 模型 指标 | 无防御 | 5 epochs | 10 epochs ASR@5(%) | 15 epochs | 20 epochs |
|:---:|:---:|:---:|:---:|:---:|:---:|
| Base(BadCLIP) | 99.94 | 85.26 | 73.09 | 55.19 | 39.23 |
| Base+Text | 99.95 | 99.49 | 98.43 | 97.59 | 95.57 |
| Base+ LT2T LMPE | 99.95 | 99.01 | 99.01 | 98.66 | 95.12 |
| DOBACLIP | 99.97 | 99.71 | 99.44 | 98.98 | 98.45 |

#### 4.4.2 触发器分布

本节对比了 BadCLIP 和 DOBACLIP 的触发器在特征空间的位置。图 6 展示了 t-SNE 和 PCA 触发嵌入的分布变化。这里面 t-SNE 用于可视化两种方法的簇结构和紧凑性；PCA 标记每个簇的中心 $\mu$ 以及它到目标类别的 $\mu$ 在干净的子空间中的距离。从结果里面可以看出 DOBACLIP 的团簇更紧密，椭圆壳面积更小，中心 $\mu$ 更接近干净分布。

从图中可以看出 BadCLIP 的触发器簇的均值与方差分别是 15.472±6.231，而 DOBACLIP 的触发器簇的均值与方差分别是 14.684±6.432。由此可以得出 DOBACLIP 的触发器在结构上更加紧凑。在与目标中心的距离上，BadCLIP 的触发器类与目标类中心距离为 0.857，而 DOBACLIP 的触发器类与目标类中心距离为 0.852。由此可以得出 DOBA 的触发器与目标类更加相似。

因为 DOBACLIP 的触发器拥有着更加紧密的分布与与目标类别更近的距离，这也就能够从源头去解释清楚为什么本文的 DOBACLIP 方法有能力巧妙地躲过那些检测手段的盘查，并且还能够在遭遇强度更大的防御措施时候依然可以保持稳定。

**图 6 触发器分布检测结果图**

### 4.5 本章小结

在本章中，本文详细阐述了 DOBACLIP 的实验设计方式，采用的数据集以及实验超参数设置。本文还展示了对比实验、消融实验的结果，并对这些结果进行了分析。通过实验结果可以看出，相比其他基线模型，DOBACLIP 拥有着最高的攻击成功率，以及在防御检测下的最好隐蔽性，以及在微调防御下的最优持久性。

---

## 5 结论与展望

### 5.1 主要结论

本文的重心放在了 MCL 的后门攻击，特别是 CLIP 模型。在 BadCLIP 基础上，本文从一个全新的、强调跨模态之间需要彼此协同配合的视角出发，引入了文本侧语义增强机制、触发模式策略优化两个改进，提出了 DOBACLIP。

大量的实验结果表明，与现在的方法相比较本文的方法在多个指标上均取得了较大的提升：在无防御场景下本文实现了更高的攻击成功率；在微调防御条件下本文表现出更强的攻击持久性；后门检测机制下本文具备更好的隐蔽性。这些结果说明了单纯依赖视觉触发器的攻击方式不能够在复杂多模态场景下让模型保持稳定，相反通过对数据分布与跨模态对齐机制的联合投毒才可以明显增强后门攻击的有效性与鲁棒性。

### 5.2 研究展望

在后门攻击方面，本文虽然在基线模型上做到了提升，但还是有着一些不足和缺陷等待进一步研究：

1. 经过优化的触发器虽然在视觉语义上更加隐蔽，但在现实层面，尤其是人类视觉层面，人类依然能够通过肉眼在图像中找到。能否设计一种隐蔽形式，使得触发器无论在机器视觉还是人类视觉都不可见。
2. 本文的文本池依赖数据集的选取，能否设计一种能够适配现市面上大多图文数据集的文本生成范式。
3. 在攻击能力层面，本文曾提到攻击者作为数据集发布者时的后门场景，做出了微调 CLIP 时采用 InfoNCE 的假设。但没有研究数据集使用者采用其他损失函数进行微调的情况。

另外，后门攻击的研究永远是服务于后门防御的。文本提出的双端优化投毒策略可以免疫现有的微调防御机制，那么今后的研究就应该思考应该设计怎么样的防御来应对这类与原始数据集有着极高相似度的中毒图文对。

---

## 参考文献

[1] Huang H, Erfani S, Li Y, et al. Detecting backdoor samples in contrastive language image pretraining[J]. arXiv preprint arXiv:2502.01385, 2025.

[2] Singh A, Hu R, Goswami V, et al. Flava: A foundational language and vision alignment model[C]//Proceedings of the IEEE/CVF conference on computer vision and pattern recognition. 2022: 15638-15650.

[3] Li J, Selvaraju R, Gotmare A, et al. Align before fuse: Vision and language representation learning with momentum distillation[J]. Advances in neural information processing systems, 2021, 34: 9694-9705.

[4] Yao L, Huang R, Hou L, et al. Filip: Fine-grained interactive language-image pre-training[J]. arXiv preprint arXiv:2111.07783, 2021.

[5] Liang J, Liang S, Liu A, et al. Poisoned forgery face: Towards backdoor attacks on face forgery detection[J]. arXiv preprint arXiv:2402.11473, 2024.

[6] Liang J, Liang S, Liu A, et al. Vl-trojan: Multimodal instruction backdoor attacks against autoregressive visual language models[J]. International Journal of Computer Vision, 2025, 133(7): 3994-4013.

[7] Zhang X, Liu A, Zhang T, et al. Towards robust physical-world backdoor attacks on lane detection[C]//Proceedings of the 32nd ACM International Conference on Multimedia. 2024: 5131-5140.

[8] Zhu M, Liang S, Wu B. Breaking the false sense of security in backdoor defense through re-activation attack[J]. Advances in Neural Information Processing Systems, 2024, 37: 114928-114964.

[9] Liu A, Zhou Y, Liu X, et al. Compromising embodied agents with contextual backdoor attacks[J]. arXiv preprint arXiv:2408.02882, 2024.

[10] Xiao Y, Liu A, Zhang X, et al. Bdefects4nn: A backdoor defect database for controlled localization studies in neural networks[C]//2025 IEEE/ACM 47th International Conference on Software Engineering. IEEE, 2025: 3123-3135.

[11] Liu X, Liang S, Han M, et al. Elba-bench: An efficient learning backdoor attacks benchmark for large language models[C]//Proceedings of the 63rd Annual Meeting of the Association for Computational Linguistics. 2025: 17928-17947.

[12] Liu M, Liang S, Howlader K, et al. Natural reflection backdoor attack on vision language model for autonomous driving[J]. arXiv preprint, 2024.

[13] Yang F, Liang S, Liu A, et al. ME: Trigger Element Combination Backdoor Attack on Copyright Infringement[J]. arXiv preprint arXiv:2506.10776, 2025.

[14] Liang S, Zhu M, Liu A, et al. Badclip: Dual-embedding guided backdoor attack on multimodal contrastive learning[C]//Proceedings of the IEEE/CVF conference on computer vision and pattern recognition. 2024: 24645-24654.

[15] Liu K, Liang S, Liang J, et al. Efficient backdoor defense in multimodal contrastive learning: A token-level unlearning method for mitigating threats[J]. arXiv preprint arXiv:2409.19526, 2024.

[16] Gu T, Dolan-Gavitt B, Garg S. Badnets: Identifying vulnerabilities in the machine learning model supply chain[J]. arXiv preprint arXiv:1708.06733, 2017.

[17] Chen X, Liu C, Li B, et al. Targeted backdoor attacks on deep learning systems using data poisoning[J]. arXiv preprint arXiv:1712.05526, 2017.

[18] Bansal H, Singhi N, Yang Y, et al. Cleanclip: Mitigating data poisoning attacks in multimodal contrastive learning[C]//Proceedings of the IEEE/CVF International Conference on Computer Vision. 2023: 112-123.

[19] Krizhevsky A, Hinton G. Learning multiple layers of features from tiny images[J]. 2009.

[20] Deng J, Dong W, Socher R, et al. Imagenet: A large-scale hierarchical image database[C]//2009 IEEE conference on computer vision and pattern recognition. Ieee, 2009: 248-255.

[21] Plummer B A, Wang L, Cervantes C M, et al. Flickr30k entities: Collecting region-to-phrase correspondences for richer image-to-sentence models[C]//Proceedings of the IEEE international conference on computer vision. 2015: 2641-2649.

[22] Yao X, Zhao H, Chen Y, et al. ToxicTextCLIP: Text-Based Poisoning and Backdoor Attacks on CLIP Pre-training[J]. arXiv preprint arXiv:2511.00446, 2025.

[23] Li Y, Liang F, Zhao L, et al. Supervision exists everywhere: A data efficient contrastive language-image pre-training paradigm[J]. arXiv preprint arXiv:2110.05208, 2021.

[24] Lee J, Kim J, Shon H, et al. Uniclip: Unified framework for contrastive language-image pre-training[J]. Advances in Neural Information Processing Systems, 2022, 35: 1008-1019.

[25] Goel S, Bansal H, Bhatia S, et al. Cyclip: Cyclic contrastive language-image pretraining[J]. Advances in Neural Information Processing Systems, 2022, 35: 6704-6719.

[26] 汪旭童, 尹捷, 刘潮歌, 等. 神经网络后门攻击与防御综述[J]. 计算机学报, 2024, 47(8): 1713-1743.

[27] 江钦辉, 李默涵, 孙彦斌. 深度神经网络后门防御综述[J]. Journal of Cyber Security, 2024, 9(4): 47-63.

[28] 钱汉伟, 孙伟松. 深度神经网络中的后门攻击与防御技术综述[J]. Journal of Frontiers of Computer Science & Technology, 2023, 17(5).

[29] 郭晶晶, 刘玖樽, 马勇, 等. 基于模型水印的联邦学习后门攻击防御方法[J]. 计算机学报, 2024, 47(3): 662-676.

[30] Carlini N, Terzis A. Poisoning and backdooring contrastive learning[J]. arXiv preprint arXiv:2106.09667, 2021.

[31] Jia J, Liu Y, Gong N Z. Badencoder: Backdoor attacks to pre-trained encoders in self-supervised learning[C]//2022 IEEE Symposium on Security and Privacy (SP). IEEE, 2022: 2043-2059.

[32] Wang Q, Yin C, Fang L, et al. GhostEncoder: Stealthy backdoor attacks with dynamic triggers to pre-trained encoders in self-supervised learning[J]. Computers & Security, 2024, 142: 103855.

[33] Tao G, Wang Z, Feng S, et al. Distribution preserving backdoor attack in self-supervised learning[C]//2024 IEEE Symposium on Security and Privacy (SP). IEEE, 2024: 2029-2047.

[34] Vijayakumar A, Cogswell M, Selvaraju R, et al. Diverse beam search for improved description of complex scenes[C]//Proceedings of the AAAI Conference on Artificial Intelligence. 2018, 32(1).

[35] Liang S, Jing Y, Wang Y, et al. BadCLIP++: Stealthy and Persistent Backdoors in Multimodal Contrastive Learning[J]. arXiv preprint arXiv:2602.17168, 2026.

[36] Feng S, Tao G, Cheng S, et al. Detecting backdoors in pre-trained encoders[C]//Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition. 2023.

---

## 致谢

行文至此，落笔为终。

首先，我要衷心感谢我的导师淦艳老师。无论是从大一入学时的科研入门，还是大四毕业设计时的选题定题、实验设计以及论文编写，淦老师始终给予了我耐心的指导，不厌其烦地指出问题。您严谨治学的态度将是我今后学习与工作的榜样。

其次，我要感谢大一到大四以来各个学科的任课老师。你们的课程逐步将我带入计算机这个专业，并让我具备了在计算机各个方向进行学习的能力。也要感谢我的辅导员邹老师，感谢您一直以来在生活、学业上的引导。

再者，我要感谢我的朋友们以及我的恋人，你们的陪伴让艰苦漫长的科研之路有了一丝乐趣。

最后，感谢我的家人一直以来的理解与支持，让我的求学之路上没有后顾之忧。家里的热闹与温馨，是我疲惫时的港湾，也是我不断前行的最大动力。

---

## 原创性声明

郑重声明：所呈交的论文（设计）《面向视觉-语言模型的后门攻击方案设计与实现》，是本人在导师的指导下，独立进行研究取得的成果。除论文（设计）中已经标注引用的内容外，本论文（设计）不包含其他人或集体已经发表或撰写过的作品成果。对本文的研究做出贡献的个人和集体，均已在文中以明确方式标明。本人完全意识到本声明的法律后果，并承诺因本声明而产生的法律结果由本人承担。

论文（设计）作者签名：__________  
日期：__________

### 使用授权书

本论文（设计）作者完全了解学校有关保留、使用论文（设计）的规定，同意学校保留并向国家有关部门或机构送交论文（设计）复印件和电子版，允许论文（设计）被查阅和借阅。本人授权重庆大学将本论文（设计）的全部或部分内容编入有关数据库进行检索，可以采用影印、缩印或扫描等复制方式保存和汇编本论文（设计）。

本论文（设计）属于：  
保密 □ 在 ____ 年解密后适用本授权书  
不保密 ☑

论文（设计）作者签名：__________ 指导教师签名：__________  
日期：__________ 日期：__________
